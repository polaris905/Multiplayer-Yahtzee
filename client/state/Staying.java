package client.state;

import client.controller.GameController;

/**
 * The type Staying that represents the staying state.
 */
public class Staying extends ClientState {

  /**
   * Instantiates a new Staying state object.
   *
   * @param controller the controller
   */
  public Staying(GameController controller) {
    super(controller);
  }
}
